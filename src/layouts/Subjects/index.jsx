import React from 'react';
import { Outlet } from 'react-router-dom';

/**
 * Subjects Component
 * This component serves as a container for rendering nested routes related to subjects.
 *
 * @returns {JSX.Element} The JSX element representing the Subjects component.
 */
const Subjects = () => {
  return <Outlet />;
};

export default Subjects;
